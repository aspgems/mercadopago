namespace :deploy do
  desc 'Copy robots.txt file for instance'
  task :robots do
    on roles(:app), in: :parallel do
      execute :cp, "#{current_path}/public/robots.#{fetch(:instance)}.txt", "#{current_path}/public/robots.txt"
    end
  end

  before :finished, :robots
end
